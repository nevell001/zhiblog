# Docker Compose - 基于 Release 产物的部署编排（不重新构建源码）
#
# 用法：
#   1. 下载 release 附件 zhi-admin.jar / frontend-dist.zip / deploy-assets.zip
#   2. 按 docs/DEPLOYMENT_RELEASE.md 整理到 ./deploy/ 并解压
#   3. cp deploy/.env.example .env  并设置强密码 / R_TOKEN_SECRET
#   4. docker compose -f docker-compose.release.yml up -d

services:
  zhi-admin:
    image: eclipse-temurin:17-jre
    container_name: zhi-admin
    command: ["java", "-jar", "/app/zhi-admin.jar"]
    ports:
      - "127.0.0.1:8080:8080"  # 仅本地访问，对外走 nginx
    volumes:
      - ./deploy/zhi-admin.jar:/app/zhi-admin.jar:ro
      - ./uploadPath:/app/uploadPath
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      TZ: Asia/Shanghai
      LANG: C.UTF-8
      LC_ALL: C.UTF-8
      DB_HOST: mysql
      DB_USERNAME: ${DB_USERNAME:-zhiblog_app}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME:-zhiblog}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      R_TOKEN_SECRET: ${R_TOKEN_SECRET}
      DRUID_USERNAME: ${DRUID_USERNAME:-nevell}
      DRUID_PASSWORD: ${DRUID_PASSWORD}
      SPRING_PROFILES_ACTIVE: prod
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/8080 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - zhi-network
    restart: unless-stopped

  mysql:
    image: mysql:8.4
    container_name: mysql
    restart: always
    command: >
      --mysql_native_password=ON
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --max_connections=200
      --innodb_buffer_pool_size=256M
    environment:
      TZ: Asia/Shanghai
      LANG: C.UTF-8
      LC_ALL: C.UTF-8
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME:-zhiblog}
      MYSQL_USER: ${DB_USERNAME:-zhiblog_app}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "127.0.0.1:3306:3306"  # 仅本地访问
    volumes:
      - mysql_data:/var/lib/mysql
      - ./deploy/sql:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD}"]
      timeout: 20s
      retries: 20
      interval: 10s
      start_period: 40s
    networks:
      - zhi-network

  redis:
    image: redis:7.2-alpine
    container_name: redis
    restart: always
    command: >
      sh -c '
        if [ -n "${REDIS_PASSWORD:-}" ]; then
          redis-server --requirepass "${REDIS_PASSWORD}" --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
        else
          redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
        fi
      '
    ports:
      - "127.0.0.1:6379:6379"  # 仅本地访问
    volumes:
      - redis_data:/data
    healthcheck:
      test: >
        sh -c '
          if [ -n "${REDIS_PASSWORD:-}" ]; then
            redis-cli -a "${REDIS_PASSWORD}" ping
          else
            redis-cli ping
          fi
        '
      interval: 10s
      timeout: 3s
      retries: 3
    networks:
      - zhi-network

  zhi-ui:
    image: nginx:1.27-alpine
    container_name: zhi-ui
    ports:
      - "80:80"
    depends_on:
      zhi-admin:
        condition: service_healthy
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./deploy/frontend-dist:/usr/share/nginx/html:ro
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - zhi-network
    restart: unless-stopped

volumes:
  mysql_data:
  redis_data:

networks:
  zhi-network:
    driver: bridge
