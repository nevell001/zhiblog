<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhi.system.mapper.BlogDailyStatsMapper">

    <insert id="upsertDailyStat">
        INSERT INTO blog_article_daily_stats (article_id, stat_date, pv, uv)
        VALUES (#{articleId}, #{statDate}, #{pv}, #{uv})
        ON DUPLICATE KEY UPDATE pv = VALUES(pv), uv = VALUES(uv), update_time = NOW()
    </insert>

    <select id="selectDailyAggregate" resultType="java.util.Map">
        SELECT stat_date AS statDate, SUM(pv) AS pv, SUM(uv) AS uv
        FROM blog_article_daily_stats
        WHERE stat_date &gt;= #{startDate}
        GROUP BY stat_date
        ORDER BY stat_date ASC
    </select>
</mapper>
