# ZhiBlog 发布流水线：打 v* 开头的 tag 时触发（如 v1.3.7），构建测试 + 自动创建 GitHub Release + 部署骨架
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: 测试与构建
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - name: Maven 编译与测试
        run: mvn -B clean verify
      - name: 上传后端产物
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: zhi-admin/target/zhi-admin.jar
          if-no-files-found: error
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: zhi-ui/package-lock.json
      - name: 前端依赖安装
        run: npm ci
        working-directory: zhi-ui
      - name: 前端检查与测试
        run: npm run lint:check && npm run format:check && npm run test && npm run build:prod
        working-directory: zhi-ui
      - name: 上传前端产物
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: zhi-ui/dist
          if-no-files-found: error

  publish:
    name: 发布 GitHub Release
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: backend-jar
          path: release/backend
      - uses: actions/download-artifact@v4
        with:
          name: frontend-dist
          path: release/frontend
      - name: 打包前端产物
        run: cd release/frontend && zip -qr ../frontend-dist.zip .
      - name: 创建 GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --repo "${GITHUB_REPOSITORY}" \
            release/backend/zhi-admin.jar \
            release/frontend-dist.zip \
            --title "ZhiBlog ${GITHUB_REF_NAME}" \
            --notes "ZhiBlog ${GITHUB_REF_NAME} 自动发布构建（GitHub Actions 生成）。"

  # ------------------------------------------------------------------
  # 部署骨架：在 GitHub 仓库 Settings -> Secrets and variables 配置
  # SERVER_HOST / SERVER_USER / SSH_PRIVATE_KEY 后，取消下方注释即可。
  # 敏感信息请全部走 Secrets，不要写进本文件。
  # ------------------------------------------------------------------
  # deploy:
  #   name: 发布部署
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/download-artifact@v4
  #       with:
  #         name: backend-jar
  #         path: deploy/backend
  #     - uses: actions/download-artifact@v4
  #       with:
  #         name: frontend-dist
  #         path: deploy/frontend
  #     - name: SSH 部署
  #       uses: appleboy/ssh-action@v1.2.0
  #       with:
  #         host: ${{ secrets.SERVER_HOST }}
  #         username: ${{ secrets.SERVER_USER }}
  #         key: ${{ secrets.SSH_PRIVATE_KEY }}
  #         script: |
  #           cd /path/to/zhiblog
  #           docker compose -f docker-compose.prod.yml pull
  #           docker compose -f docker-compose.prod.yml up -d
